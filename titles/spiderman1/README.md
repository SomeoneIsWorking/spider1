# Spider-Man (`SLUS_008.75`)

Current implemented target (USA). `SLUS_008.75`, the disc boot executable/serial, is its canonical
title key. Its seam and generated substrate currently live in the repository-level `game/` and
`generated/` directories. They move under the per-title layout when `SLUS_013.78` implementation
begins.
